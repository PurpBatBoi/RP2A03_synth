//! rp2a03_common\src\format\mod.rs
//! File-format implementations for save/load. The native `.rp2a03patch`
//! format lives in `patch`; a `.fti` FamiTracker importer is planned as a
//! sibling module here later (out of scope for now).

pub mod patch;
